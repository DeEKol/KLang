{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '\\.(ts|tsx|js|mjs)$' | grep -v 'node_modules' | xargs -I{} sh -c 'npx eslint --fix {} 2>/dev/null; npx prettier --write {} 2>/dev/null' || true"
          }
        ]
      }
    ]
  }
}
